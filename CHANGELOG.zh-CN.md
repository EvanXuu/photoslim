# 更新日志

[English](CHANGELOG.md)

## [0.2beta] - 2026-08-19

PhotoSlim 0.2beta 将压缩流程改为“先审核、后写入”：在用户查看并确认压缩结果前，原件不会被改动。

### 新功能

- **写入前审核**：压缩结果先保存在本机审核网格中。用户可以打开结果、放大查看、对比鼠标悬浮位置的原图，确认后才写入 Photos。
- **iCloud 并行下载**：最多同时下载 5 个原件，并与本机压缩重叠进行。
- **`hvc1` HEVC 重压缩**：普通 `hvc1` HEVC 视频可以明确加入任务并重新编码。Dolby Vision、HDR、`hev1`、HEVC Alpha 和混合轨道仍保守排除。
- **自动视频导出**：AVAssetExportSession 的自动 HEVC 路径现在是默认选项；手动模式提供 1080p、2160p、4320p 及 30/60 fps 的可编辑码率表。
- **原生窗口信息**：窗口标题显示当前媒体类型，副标题显示项目数量，不再在工具栏重复放置标题控件。

### 改进

- 扫描、选择和后台空间校验不再显示预计输出大小或预计节省；只有实际生成并验证输出后才记录真实节省空间。
- 新建或未被用户修改过的旧设置，默认最低实际节省比例改为 8%。
- Photos 提供原始资源 `dataSize` 时直接使用；大小未知的 iCloud 项目不再估算。
- 保留增量扫描、搜索、筛选、排序、置顶、列表/网格、会话恢复、统计和任务历史，同时简化面向用户的文案。
- macOS 排序控件会直接列出排序方式；空间校验不再展示独立预检界面，图库底栏改为显示设备可用空间与总容量。
- iPhone 选中项目后隐藏工作区栏并显示胶囊操作栏；iOS 26 使用原生 Liquid Glass，较早的受支持系统使用系统材质。

### 修复

- 按键对比原图时跟随鼠标悬浮项目；未聚焦输入框时不再发出按键提示音。
- 压缩结果可以打开为可缩放的详情视图。
- 移除可能叠加在原生 macOS 工具栏上的旧搜索和标题样式。

### 注意事项

- 预编译包使用 ad-hoc 签名且未经过 Apple 公证，macOS 可能显示未知开发者警告。
- PhotoKit 新建资产会获得新的资产 ID 和添加日期；编辑历史及人物关系不会复制。
- 仍建议先使用少量、有备份的项目测试；这是早期 Alpha 质量的版本。

## [0.1.0-alpha.1] - 2026-08-10

首个公开 Alpha 版本，用于在少量、有备份的图库中验证扫描、下载、压缩、审核和 Photos 删除确认的完整流程。

### 新功能

- 扫描 Apple 照片图库，并通过增量索引减少后续扫描时间。
- 将普通 JPEG 转换为 HEIC，将 SDR H.264 或普通 `hvc1` HEVC 视频转换为 HEVC。
- 最多并行下载 5 个 iCloud 原件，并显示独立的下载和压缩进度。
- 写入 Photos 前在本机审核压缩结果。
- 跨次启动保存任务队列、处理账本、统计和未完成会话。

### 改进

- 提供手动视频码率、关键帧、帧重排和音频策略设置。
- 下载原件后再读取 iCloud 文件的真实大小。
- 结果详情支持 100%–500% 缩放、捏合缩放和拖动平移。
- 反斜杠快捷键只对鼠标悬浮的结果生效。

### 修复

- 修复对比原图时未聚焦文本框产生按键提示音的问题。
- 修复压缩结果无法打开可缩放详情的问题。
- 修复网格和详情预览争抢同一键盘事件的问题。

### 注意事项

- 预编译包使用 ad-hoc 签名且未经过 Apple 公证。
- Dolby Vision、HDR、`hev1`、HEVC Alpha 和混合轨道默认排除。
- 该版本面向少量、有备份的测试项目。

[0.2beta]: https://github.com/EvanXuu/photoslim/releases/tag/v0.2beta
[0.1.0-alpha.1]: https://github.com/EvanXuu/photoslim/releases/tag/v0.1.0-alpha.1
