# PRD: PhotoSlim（暂定名）

> 状态：产品需求草案 v0.2  
> 平台：macOS 原生应用  
> 当前阶段：需求确认，尚未进入正式产品实现  
> 核心原则：本地处理、先验证后替换、任何删除都可恢复

## 1. Summary

PhotoSlim 是一款原生 macOS 照片与视频压缩工具。它通过公开的 PhotoKit、AVFoundation 和 ImageIO 接口扫描 Apple 照片图库，找出占用空间较大的旧格式媒体，在本机转换为 HEIF/HEIC 或 HEVC。压缩结果先进入本地预览网格，用户确认后才导入并验证副本，然后要求用户选择“撤回压缩副本”或“确认删除原件”。

产品首先解决“iCloud 照片占用很大，但用户不敢用脚本批量替换”的问题。它不直接修改 `.photoslibrary` 数据库，不承诺保留公开 PhotoKit 无法设置的数据，也不会自动永久清空“最近删除”。

## 2. Contacts

| 角色 | 负责人 | 职责 |
|---|---|---|
| 产品负责人 | 用户，待确认姓名 | 确认目标用户、首版范围和风险接受度 |
| 产品与工程协作 | Codex | PRD、原生架构、实现和验证 |
| 视觉与交互设计 | 待定 | 设计系统、交互原型、可访问性 |
| 测试负责人 | 待定 | 真实图库、iCloud、异常恢复和回归测试 |
| 隐私与发布 | 待定 | 沙盒权限、签名、公证和上架策略 |

## 3. Background

### 3.1 用户问题

很多旧 iPhone、相机、屏幕录制和导入媒体仍使用 JPEG、H.264 或更旧格式。它们在视觉质量相近时，通常比 HEIF/HEIC 和 HEVC 占用更多空间。Apple 照片没有提供“批量转为新编码并安全替换原件”的完整工作流。

用户目前面临四个困难：

1. 不知道哪些媒体值得压缩，以及大约能节省多少空间。
2. iCloud 优化存储可能只在本机保留低分辨率版本，处理时可能触发原件下载。
3. 新建压缩资产后，拍摄日期、位置、收藏、相簿归属等信息可能丢失。
4. PhotoKit 不能原地替换资源；新建资产会改变资产 ID 和“添加日期”，删除原资产也有数据安全风险。

### 3.2 当前原型已经验证的能力

现有命令行式原型已经完成两次真实 PhotoKit 测试：

- 将一个 H.264 视频转换为 HEVC，创建新资产并把原资产移入“最近删除”。
- 对一个收藏视频执行同样流程，并成功保留收藏状态、拍摄日期、位置和隐藏状态。
- PhotoKit 返回的新资产可再次读取并验证编码。
- 删除通过 PhotoKit 执行，可从“最近删除”恢复。

这些测试证明核心路径可行，但原型仍缺少稳定签名、持久任务状态、完整验证、照片压缩、批量处理和产品级 UI。

### 3.3 平台边界

正式产品只使用 Apple 公开接口，不直接读取或修改 Photos 数据库。

公开 PhotoKit 无法保证复制以下数据：

- 原资产的 `localIdentifier`
- 原资产的“添加日期”
- 可逆编辑历史与调整数据
- 人脸识别、人物关系、回忆归属和内部机器学习结果
- 某些共享相簿和系统内部关系

因此，产品文案必须使用“创建压缩副本并替换原资产”，不能声称“原地改写”或“所有信息完全不变”。

### 3.4 Why now

现有原型已经验证最危险的 PhotoKit 创建与删除路径。下一步的价值不在于继续写一次性脚本，而是把安全检查、权限、iCloud 下载、恢复和用户决策做成可靠产品。

## 4. Objective

### 4.1 产品目标

让用户在不理解编码技术的情况下，安全地识别、预览和压缩 Apple 照片图库中的旧格式照片与视频，并清楚知道：

- 哪些项目会被处理。
- 能节省多少空间。
- 哪些元数据可以保留。
- 哪些信息一定会改变。
- 发生中断或误操作时如何恢复。

### 4.2 非目标

首版不做以下事情：

- 不直接编辑 `.photoslibrary` 文件或数据库。
- 不永久清空“最近删除”。
- 不承诺保留添加日期、资产 ID、编辑历史或人脸数据。
- 不做云端上传、在线转码或用户媒体分析。
- 不做重复照片整理、内容分类或 AI 相册管理。
- 不处理 RAW/ProRAW、Live Photo、空间视频、电影效果视频等高风险类型的替换。
- 不把已经是 HEIF/HEVC 的媒体默认再次有损压缩。

### 4.3 Key Results

以下为首轮 Beta 的 SMART 目标，数值需要通过真实图库测试校准：

| 目标 | 衡量方式 | Beta 门槛 |
|---|---|---|
| 安全 | 未通过全部验证时移动原件的次数 | 0 次 |
| 元数据 | 支持字段在替换前后的匹配率 | 100% |
| 兼容性 | P0 支持类型的成功完成率 | 不低于 95% |
| 稳定性 | 无崩溃会话比例 | 不低于 99.5% |
| 节省空间 | 合格项目的中位节省比例 | 暂定不低于 25% |
| 可恢复 | 中断任务可识别并安全续作的比例 | 100% |
| 用户理解 | 测试用户理解“添加日期和资产 ID 会变化” | 不低于 90% |
| 隐私 | 离开本机的照片、视频或精确位置数据 | 0 |

## 5. Market Segments

市场按用户要完成的任务划分，而不是按年龄划分。

### 5.1 主要用户：iCloud 空间管理者

任务：在不手工导出和重新导入整个图库的情况下，减少旧媒体占用。

约束：图库大、部分原件只在 iCloud、害怕丢失时间和位置信息。

### 5.2 次要用户：家庭照片维护者

任务：整理多年累积的 iPhone 视频，同时保留收藏和相簿结构。

约束：媒体有情感价值，容错要求极高，通常不懂编码参数。

### 5.3 高级用户：摄影与内容创作者

任务：筛选可安全压缩的普通交付照片、屏幕录制和旧视频，把 RAW、HDR 和重要母版排除在外。

约束：需要精确过滤、可审计报告、批量队列和质量控制。

### 5.4 不优先服务的用户

- 需要无损归档原始素材的专业档案馆。
- 需要跨平台图库管理的用户。
- 希望绕过 Apple 权限或直接修改 Photos 数据库的用户。

## 6. Value Propositions

### 6.1 核心价值

“在 Mac 上看清风险和收益后，再安全压缩 Apple 照片图库。”

### 6.2 用户获得

- 清楚的可压缩媒体清单，以及任务完成后可核对的真实节省空间。
- 默认保守的质量策略，不要求用户理解码率和编码器。
- 拍摄日期、位置、收藏、隐藏状态和普通相簿关系的自动复制与验证。
- 对 iCloud 原件下载、临时空间和处理进度的明确提示。
- 可恢复的替换流程和完整处理记录。

### 6.3 用户避免

- 直接操作 Photos 数据库导致图库损坏。
- 盲目批量压缩后才发现 HDR、编辑或元数据丢失。
- 因网络或应用退出留下无法识别的重复副本。
- 误以为“添加日期”和资产 ID 可以完全保留。

### 6.4 信任原则

1. 默认只分析，不修改。
2. 检查阶段原件保持不动；只有用户选择“确认删除原件”后才移动原件。
3. 导入和验证成功前绝不移动原件。
4. 永不自动永久删除“最近删除”中的内容。
5. 每个跳过、失败和改变都必须可解释。

## 7. Solution

### 7.1 UX and Prototypes

#### 核心用户流程

```text
[欢迎与隐私说明]
          |
          v
[授权全部照片] -> [扫描图库，不下载云端原件]
          |                     |
          |                     v
          |              [可压缩项目清单]
          |                     |
          |                     v
          |       [筛选 + 排序 + 网格/列表 + 原件大小]
          |                     |
          |                     v
          |          [推荐参数，按需进入详细设置]
          |                     |
          |                     v
          +------------> [下载原件 + 转码]
                                |
                                v
                  [任务结束后先生成本地结果 + 文件验证]
                                |
                                v
                  [本地网格预览；锁定新任务]
                                |
                       +--------+---------+
                       |                  |
                       v                  v
                [撤回压缩副本]     [确认删除原件]
                       |                  |
                       v                  v
                [新副本进最近删除] [原件进最近删除]
                       |                  |
                       +--------+---------+
                                v
                         [结果与恢复说明]
```

#### 首页

```text
+------------------------------------------------------------------+
| PhotoSlim                                      Photos: 已授权     |
+------------------------------------------------------------------+
| 原件与已完成任务统计                                           > |
| 真实节省空间                                                   |
|                                                                  |
| [照片 8,420] [视频 612] [仅 iCloud 301] [不支持 94]              |
|                                                                  |
|  可处理项目                                                     |
|  旧格式视频        286 个       原件大小待选择时核对 [查看]       |
|  JPEG 照片       7,943 个       原件大小待选择时核对 [查看]       |
|                                                                  |
|                         [扫描变更]  [开始选择]                     |
+------------------------------------------------------------------+
```

#### 结果与选择页

```text
+------------------------------------------------------------------+
| < 返回     可压缩项目                         已选 124 / 9.6 GB   |
+------------------------------------------------------------------+
| 筛选: [时间] [大小] [收藏] [相簿] [本地/iCloud]                  |
| 排序: [原文件大小 v]                             [网格] [列表]    |
| > 排除项（默认折叠，当前排除 7 类）                               |
+------------------------------------------------------------------+
| [x] 缩略图  2018 屏幕录制   H.264 -> HEVC   原件 480 MB  收藏     |
| [x] 缩略图  2016 照片       JPEG  -> HEIC   原件 12 MB   本地     |
| [ ] 缩略图  2021 HDR 视频    暂不支持                    跳过     |
+------------------------------------------------------------------+
| 压缩参数: 推荐  照片 82 / 视频 80 / 保持尺寸与帧率    [编辑...]   |
| 任务结束后将自动导入并进入强制检查阶段                            |
|                                               [开始处理]          |
+------------------------------------------------------------------+
```

#### 处理页

```text
+------------------------------------------------------------------+
| 正在处理 32 / 124                                [暂停] [停止]    |
+------------------------------------------------------------------+
| 当前: IMG_2018.MOV                                                |
| 下载原件  [##########] 100%                                      |
| 转码      [######....]  63%                                      |
|                                                                  |
| 已节省 2.1 GB    成功 31    跳过 0    失败 0                      |
| 临时空间剩余 41 GB                                               |
+------------------------------------------------------------------+
```

#### 任务完成后的强制检查页

```text
+------------------------------------------------------------------+
| 需要检查：124 个压缩副本                       下一任务已锁定     |
+------------------------------------------------------------------+
| 新资产已经导入并通过自动验证，原件尚未删除。                     |
| 请先在 Photos 中抽查画质、声音、日期、位置和相簿。                |
|                                                                  |
| [在 Photos 中查看新资产]                                         |
|                                                                  |
| [撤回压缩副本]                         [确认删除 124 个原件]      |
|                                                                  |
| 在作出选择前，退出 App 需要连续确认两次；会话会在重启后恢复。     |
+------------------------------------------------------------------+
```

### 7.2 Key Features

#### F1. 首次启动与权限

- 用一屏解释为什么需要“所有照片”权限。
- 说明媒体只在本机处理。
- 显示当前权限状态，并在拒绝后提供“打开系统设置”。
- 使用固定 Bundle ID 和稳定签名身份；应用升级不得因临时签名变化重复请求权限。
- 未授权时可以浏览演示界面，但不能扫描真实图库。

验收标准：

- 用户拒绝权限后，应用不会卡住或反复弹窗。
- 用户从系统设置恢复权限后，无需重装应用。
- 权限变化会在 UI 中立即更新。

#### F2. 安全扫描

- 使用 PhotoKit 获取媒体类型、日期、尺寸、时长、收藏、隐藏和相簿关系。
- 初始扫描不得主动下载 iCloud 原件。
- 首次扫描保存资产索引与 PhotoKit persistent change token；后续启动、手动刷新和运行期图库通知只重扫新增或更新的资产，并从索引移除已删除资产。token 过期或变更详情不可用时，先用资产 ID 与修改时间做轻量差异比较，仅对差异项目读取原件；只有索引不存在或损坏时才回退全量扫描。
- 已确认删除原件的任务必须把成功项目的源资产 ID 与压缩副本 ID 写入持久处理账本。账本命中的资产标记为“已经由 PhotoSlim 处理”，默认隐藏且即使取消排除也不能再次加入任务；撤回和失败项目不计入。
- 对视频编码信息采用分阶段扫描：本地原件遍历全部视频轨道和组合资产源片段；无法读取远端原件时标记为“编码待下载确认”。
- 照片/视频/收藏等图库类型只由侧栏切换，不在筛选菜单重复出现；筛选菜单支持收藏、相簿和本地/iCloud 状态。
- 时间筛选统一使用最低年龄门槛：“1 年以上、3 年以上、5 年以上、自定义 N 年以上”，另提供手动日期范围。默认按拍摄日期筛选；添加日期不作为可靠条件。
- 大小筛选统一使用最低体积门槛：“10 MB 以上、100 MB 以上、1 GB 以上、自定义 N MB 以上”，另提供手动最小/最大范围。
- 排序支持拍摄日期、原文件大小、时长和文件名；默认按原文件大小降序。
- 支持网格和列表两种视图，并记住用户上次选择。
- 网格和列表中的照片、视频预览必须使用 aspect-fit 完整显示；容器可以留空白，不得用 aspect-fill 裁切内容。
- 筛选菜单中设置一个默认折叠的“排除项”分组。它包含已是高效编码、编码待下载确认、已编辑资产、RAW/ProRAW、Live Photo、HDR/Dolby Vision、慢动作/电影效果/空间视频、隐藏媒体和透明或动画图片。
- 用户取消某一排除项时，先显示该类型的具体风险，再将项目显示到浏览器中。
- 取消排除只改变可见性。首版明确不支持的媒体仍显示为禁用状态，不能仅凭关闭排除强制处理。
- 对首版可以处理但风险较高的类型，例如隐藏媒体或屏幕录制，用户确认提醒后可以加入任务。
- 已是 HEIF/HEVC 的媒体默认标记为“不建议再次压缩”。
- 扫描结果写入本地缓存；Photos 变化后做增量更新。

验收标准：

- 仅扫描不会新建、编辑或删除 Photos 资产。
- 每个“不支持”项目都显示明确原因。
- 折叠“排除项”不会重置其中的选择。
- 时间、大小、排序和视图切换组合使用时，项目数量和已知原件大小必须同步更新。
- 用户取消排除后必须先看到提醒，不能直接把高风险媒体加入任务。
- 重启应用后可恢复最近一次扫描结果并检测图库变化。

#### F3. 支持类型矩阵

| 类型 | 扫描 | 首版压缩 | 首版替换 | 默认行为 |
|---|---:|---:|---:|---|
| JPEG 普通照片 | 是 | HEIC | 是 | 推荐 |
| 不含透明通道的 PNG | 是 | HEIC | 可选 | 默认谨慎提示 |
| 含透明通道或动画图片 | 是 | 否 | 否 | 跳过 |
| TIFF/BMP 等可解码静态图 | 是 | 后续 | 否 | 只报告 |
| HEIF/HEIC | 是 | 否 | 否 | 默认跳过 |
| H.264 SDR 普通视频 | 是 | HEVC | 是 | 推荐 |
| 可由 AVFoundation 解码的旧视频 | 是 | HEVC | 条件支持 | 先验证 |
| 普通 hvc1 HEVC 视频 | 是 | HEVC | 是 | 明确提示后可选 |
| hev1/Dolby Vision/HEVC Alpha | 是 | 否 | 否 | 默认跳过 |
| HDR/Dolby Vision | 是 | 后续 | 否 | 首版跳过 |
| 慢动作/电影效果/空间视频 | 是 | 后续 | 否 | 首版跳过 |
| RAW/ProRAW | 是 | 否 | 否 | 永不默认替换 |
| Live Photo | 是 | 后续 | 否 | 首版跳过 |
| 已编辑资产 | 是 | 后续 | 否 | 默认跳过并解释 |

#### F4. 实际节省空间

- 扫描和选择阶段不生成目标大小或节省空间预测。
- 对仅在 iCloud 的媒体不估算原件大小；任务下载后读取真实字节数。
- 浏览器只显示已知原件大小；任务完成后再用实际输出复核压缩率并记录真实节省空间。
- macOS 27+ 在 `PHAssetResource.dataSize` 已知时显示精确资源大小；大小未知的云端项目不显示原件大小；汇总按 PhotoKit 资产标识去重。
- 最低节省比例只在实际输出生成并验证后判断；不用于扫描或选择阶段排除项目。
- 每次新增选择后核对已知原件、已知 iCloud 下载和安全余量，并与本机可用于任务的空间比较；未知云端大小在下载后再核对。
- 单选超限时拒绝新项目；“全选”按当前排序依次保留可容纳项目，跳过超限项目后继续尝试较小项目。
- 若可用空间在选择后减少，按选择顺序从后向前取消多余项目，并要求用户重新核对后才能开始。

#### F5. 质量模式

浏览器底部只显示一行“推荐参数”和一个“编辑”按钮，避免把编码细节放在主流程中。以下数值是首轮推荐值，Phase 0 必须通过真实图库校准：

| 媒体 | 推荐参数 |
|---|---|
| 照片 | HEIC；质量 82/100；保持原始像素尺寸、方向、色彩配置和元数据 |
| 视频 | HEVC；默认使用 AVAssetExportSession 自动路径；编辑参数时可切换手动 VideoToolbox 码率表、硬上限、关键帧间隔和帧重排；保持分辨率和帧率 |
| 音频 | 能直通时保持原音频；否则使用与源声道匹配的高质量 AAC |
| 通用 | 实际节省低于 8% 时不写入；不允许输出文件大于原文件 |

点击“编辑”后，以 Sheet 形式打开详细设置：

- 照片质量、格式和是否保持原始尺寸。
- 视频质量、码率策略、最大平均码率、分辨率上限和帧率上限。
- 音频直通或 AAC 码率。
- 最低节省比例。
- 元数据保留与特殊媒体跳过规则。
- “恢复推荐参数”按钮；视频默认选择“自动”，手动码率表只在用户显式编辑时使用。

详细设置会保存为本次任务配置。创建下一任务时重新使用上次配置，但主界面仍以一行摘要展示。

产品不得把固定码率作为默认策略。推荐配置使用质量目标和可变码率，60% 只是初始上限，不是每个视频强制达到的固定码率。

#### F6. 照片压缩

- 从 PhotoKit 请求原始图像数据；只有用户开始处理时才允许下载 iCloud 原件。
- 使用 ImageIO/Core Image 编码 HEIC。
- 默认保持像素尺寸、方向、色彩空间、拍摄日期、GPS 和可公开复制的 EXIF/IPTC 元数据。
- 有透明通道、动画、RAW 或无法确认色彩空间时跳过。
- 编码后解码新文件，检查尺寸、方向、色彩配置和可读性。
- 可选做抽样感知质量检查；低于阈值时不导入。

#### F7. 视频压缩

- 使用 AVFoundation；必要时使用 AVAssetReader/Writer 和 VideoToolbox。
- 首版只处理普通 SDR 视频。
- 保持分辨率、方向、帧率模式、时长、音频轨道和 QuickTime 元数据。
- 在不能确定 HDR、色彩、时间轴或音频可完整保留时跳过。
- 输出使用 HEVC，并在导入前验证文件可从开头、中间和结尾解码。

#### F8. 替换事务

每次批量处理是一个独占会话。任务完成转码后，应用自动创建压缩资产、复制元数据并执行完整验证，不要求用户逐项点击导入。此阶段只完成“替换准备”，原资产保持不动。

每个资产和任务会话必须按以下状态机处理：

```text
discovered
    -> downloading
    -> transcoding
    -> file_verified
    -> imported
    -> photos_metadata_verified
    -> review_pending
          |
          +-> rollback_requested
          |      -> compressed_copy_moved_to_recently_deleted
          |      -> rolled_back
          |
          `-> commit_requested
                 -> original_moved_to_recently_deleted
                 -> committed
```

如果导入或验证失败，原资产保持不动，失败项目不会进入 `review_pending`。应用必须记录源资产 ID、新资产 ID、文件指纹、时间、压缩设置和当前状态。

任务进入 `review_pending` 后：

- 自动打开强制检查页，并提供“在 Photos 中查看新资产”。
- 禁止开始、导入或排队下一个任务。
- 用户必须对整个任务选择“撤回压缩副本”或“确认删除原件”。
- “撤回压缩副本”会把本任务创建的新资产移入“最近删除”，原资产不变。
- “确认删除原件”会直接发起 PhotoKit 删除事务，由 Photos 显示系统确认提示；应用不叠加第二个确认框。确认后，已成功验证项目的原资产移入“最近删除”，新资产保留。
- 两个操作都必须逐项记录结果。只要仍有未解决项目，会话就保持锁定。

公开 PhotoKit 提供删除资产接口，但本机 SDK 未提供从“最近删除”自动恢复资产的公开接口。因此，原资产在用户作出选择前保持不动。“撤回”只删除本任务创建的压缩副本，不尝试恢复任何已删除资产。

#### F8.1 退出保护与会话恢复

任务处于 `review_pending` 时，用户主动退出应用需要连续确认两次：

1. 第一次提示说明“当前替换会话尚未决定，下一任务仍会被锁定”。默认按钮是“继续检查”。
2. 用户仍选择退出后，第二次提示说明“退出不会撤回或删除任何资产；下次启动将强制恢复此会话”。

第二次确认后允许退出，但不能清除会话。崩溃、强制退出、系统重启或升级无法显示确认框，应用必须依靠持久化账本恢复。

应用重启后：

- 如果只有本地输出，删除临时文件或继续导入。
- 如果新资产已创建但原资产还在，直接恢复强制检查页，不能再次创建重复资产。
- 如果会话处于 `review_pending`，启动后不能关闭到普通首页，也不能开始新任务。
- 如果用户已经选择提交或撤回但操作未完成，继续剩余项目，不重复处理成功项目。
- 如果原资产已移入“最近删除”，确认新资产仍存在后把该项目标记为 `committed`。
- 如果新资产已移入“最近删除”，确认原资产仍存在后把该项目标记为 `rolled_back`。

#### F9. 提交删除前验证

任务可以自动进入检查阶段，但移动原资产前必须全部通过：

| 检查项 | 照片 | 视频 | 通过条件 |
|---|---:|---:|---|
| 新资产存在且可读取 | 是 | 是 | PhotoKit 可重新获取 |
| 编码格式 | 是 | 是 | HEIC/HEIF 或 HEVC |
| 像素尺寸 | 是 | 是 | 与设置一致 |
| 拍摄日期 | 是 | 是 | 精确匹配 |
| 位置 | 是 | 是 | 坐标在容差内匹配 |
| 收藏/隐藏 | 是 | 是 | 完全匹配 |
| 普通相簿关系 | 是 | 是 | 可访问相簿全部匹配 |
| 方向与色彩 | 是 | 是 | 无意外改变 |
| 时长 | 否 | 是 | 差异不超过一帧；帧率未知时不超过 50 ms |
| 音频轨道 | 否 | 是 | 轨道数、声道和时长匹配 |
| 文件可解码 | 是 | 是 | 解码测试通过 |

#### F10. 元数据保留与限制说明

| 数据 | 产品承诺 |
|---|---|
| 拍摄日期 | 复制并验证 |
| 地理位置 | 复制并验证 |
| 收藏状态 | 复制并验证 |
| 隐藏状态 | 复制并验证 |
| 普通相簿归属 | 尽可能复制并验证 |
| 文件 EXIF/QuickTime 元数据 | 尽可能复制并报告差异 |
| 添加日期 | 无法保留，处理前明确提示 |
| PhotoKit 资产 ID | 一定变化，处理前明确提示 |
| 可逆编辑历史 | 首版不支持，已编辑资产默认跳过 |
| 人脸、人物、回忆 | 不保证复制 |
| 共享相簿关系 | 首版不保证复制 |

#### F11. iCloud 行为

- 扫描阶段默认禁止网络下载。
- 项目列表显示“本地可用”“需要 iCloud 下载”或“状态未知”。
- 开始前显示预计下载数量、网络要求和临时磁盘需求。
- 下载必须有进度、暂停、取消和失败重试。
- 网络中断不会删除原资产，也不会把任务标记为完成。
- 新资产导入后由 Photos 自己同步到 iCloud；应用显示“已导入，等待 Photos 同步”，不伪造云端完成状态。
- 原资产移入“最近删除”不会立即释放全部 iCloud 空间，结果页必须说明这一点。

#### F12. 批量队列

- 支持暂停、继续和停止。
- 同一时间只允许存在一个活动任务或待检查会话。
- 当前任务进入 `review_pending` 后，队列不再启动后续任务；用户必须先提交或撤回。
- 默认串行执行 Photos 创建/删除事务；转码并发数根据硬件和可用空间限制。
- 支持电脑睡眠后恢复。
- 每个项目独立成功或失败，单个失败不破坏整个批次。
- 允许导出 CSV/JSON 处理报告，但默认不包含精确位置。

#### F13. 历史与恢复

- 显示每批处理的时间、项目数、节省空间、失败和跳过原因。
- 独立统计页显示本机总容量、已使用、立即可用、可用于任务的空间，以及当前选择所需临时空间。
- 未解决会话固定显示在历史顶部，并带有“继续检查”按钮。
- 提供“在 Photos 中查看新资产”和“打开最近删除”的入口。
- 历史明确标记“已提交替换”或“已撤回压缩副本”。
- 提交和撤回都只移动到“最近删除”，因此仍可由用户在 Photos 中人工恢复。
- “撤回压缩副本”不会恢复或操作任何已经删除的原资产。
- 应用不提供“永久删除最近删除”按钮。

#### F14. 设置

- 推荐压缩参数和上次使用的详细参数。
- 最低节省比例。
- 是否允许蜂窝热点/计量网络下载。
- 临时目录和最低剩余磁盘空间。
- 是否包含收藏、隐藏和指定相簿。
- 是否允许屏幕录制等非相机视频。
- 默认排序字段，以及网格/列表视图偏好。
- 默认排除项；修改高风险排除项仍必须在照片浏览器中再次提醒。
- 日志级别和隐私诊断导出。

#### F15. 隐私、可访问性与本地化

- 所有媒体处理均在本机进行。
- 不收集照片、视频、缩略图、EXIF 或精确位置。
- 若未来加入遥测，必须默认不包含资产 ID、文件名和位置，并提供关闭选项。
- 支持 VoiceOver、键盘导航、动态字体和高对比度。
- 首版支持简体中文和英文。

### 7.3 Technology

#### 原生技术栈

- Swift 6
- SwiftUI 为主，必要时使用 AppKit 承载系统能力
- PhotoKit 读取、创建和删除资产
- AVFoundation 与 VideoToolbox 处理视频
- ImageIO/Core Image 处理照片与 HEIC
- Core Data 或 SQLite 保存扫描索引、任务状态和恢复记录
- OSLog 保存不含敏感信息的诊断日志

#### 建议模块

```text
PhotoSlimApp
|-- PermissionService
|-- PhotoLibraryScanner
|-- EligibilityEngine
|-- SavingsEstimator
|-- ImageTranscoder
|-- VideoTranscoder
|-- MediaVerifier
|-- PhotoLibraryWriter
|-- JobQueue
|-- RecoveryLedger
|-- StorageMonitor
`-- SwiftUI Features
    |-- Onboarding
    |-- Dashboard
    |-- Results
    |-- ItemInspector
    |-- Processing
    |-- History
    `-- Settings
```

#### 权限与发布要求

- 使用稳定 Bundle ID。
- 开发阶段使用固定 Apple Development 签名；发布使用 Developer ID 或 Mac App Store 签名。
- 启用 App Sandbox 和 Photos Library entitlement。
- 版本更新保持相同 Team ID、Bundle ID 和 designated requirement，避免每次重新授权。
- 正式版必须公证；禁止长期使用 ad-hoc 签名。

#### 数据安全

- 不直接访问 Photos 数据库。
- 临时输出使用应用专属目录，文件名不包含原始精确位置。
- 任务完成或失败后清理临时媒体；清理前验证路径位于应用管理目录。
- 本地任务账本不保存图像内容。

### 7.4 Assumptions, Risks, and Open Questions

#### 已确认事实

- 公开 PhotoKit 可以创建 HEVC 视频资产并复制拍摄日期、位置、收藏、隐藏和普通相簿关系。
- 创建并验证后，可以通过 PhotoKit 把原资产移入“最近删除”。
- 当前本机 PhotoKit SDK 公开头文件提供删除资产接口，但未提供从“最近删除”恢复资产的公开接口。
- 新资产会获得新的 PhotoKit ID。
- 公开 PhotoKit 无法设置原始“添加日期”。

#### 待验证假设

- JPEG 转 HEIC 在推荐质量下可稳定达到至少 20% 的中位节省。
- H.264 转 HEVC 在推荐质量下可稳定达到至少 30% 的中位节省。
- 大型图库可以只靠增量扫描保持良好性能。
- 普通相簿关系在不同图库和 iCloud 状态下都能稳定复制。
- Photos 同步后，新资产的文件资源不会被系统再次改变编码。

#### 主要风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 新资产丢失无法公开复制的数据 | 用户认为“完全保留”但实际变化 | 明确支持矩阵，已编辑和特殊资产默认跳过 |
| iCloud 下载耗时或失败 | 队列卡住、占用临时空间 | 分阶段下载、暂停恢复、磁盘预检 |
| 应用在导入后、用户决定前退出 | 产生重复副本或遗忘原件 | 独占待检查会话、双重退出确认和持久恢复账本 |
| 删除后新资产同步失败 | 用户担心云端安全 | 删除前本地验证；原件仅进入最近删除；不永久删除 |
| HEVC/HEIC 兼容性 | 第三方设备打不开 | 兼容性提示、检查阶段保留原件、导出前验证 |
| 固定质量不适合所有内容 | 有的文件反而更大或画质下降 | 估算、最低节省阈值和逐项质量验证 |
| 应用更新反复请求权限 | 用户失去信任 | 稳定签名身份和发布流程 |

#### 本轮已确认的产品决策

- 默认排除项收纳在照片浏览器筛选菜单内，并保持折叠。
- 取消排除时先显示风险提醒。
- 浏览器加入时间、大小筛选，排序以及网格/列表切换。
- 主流程展示推荐压缩值，详细参数放入“编辑”Sheet。
- 任务结束后先创建并验证本地压缩结果，进入强制预览会话；用户确认写入后才创建和验证 Photos 资产。
- 用户必须在“撤回压缩副本”和“确认删除原件”之间作出选择，才能开始下一任务。
- 待检查会话在退出、崩溃和系统重启后恢复；主动退出需要两次确认。

#### 待修改需求清单

- [x] 搜索文件名 UI：移除旧的自绘搜索框及其背景/边框，只保留一个 Finder 风格的 Toolbar 搜索控件，并保留清空和键盘焦点。
- [x] hvc1 视频：将已识别为 `hvc1` 的 HEVC 视频加入可选的重新压缩范围；界面明确这是 HEVC→HEVC 的再次有损编码，并沿用最低节省比例、输出可解码性、尺寸和时长验证。
- [x] iCloud 并行下载：最多 5 个原件同时下载，并与当前压缩流水线重叠；受临时磁盘空间、网络失败、取消和会话恢复约束。
- [x] 写入逻辑重构：压缩完成后先进入本地压缩结果网格，不立即写入照片图库。每个结果提供按住显示对应原图的对比按钮；点击结果进入单图查看，同样支持按住按钮对比和按住 Lightroom 风格的 Before/After 反斜杠快捷键。用户确认后才通过明确的写入按钮导入压缩结果并删除原图；删除继续使用 Photos 系统确认，不增加重复的应用内确认。
- [x] 任务终止与临时文件清理：处理任务提供明确的终止操作；终止后取消进行中的 iCloud 下载和转码并删除应用管理目录中的临时生成文件。保留终止历史记录，不删除原图或已写入相册的资产；清理失败时在历史页显示并支持重试。
- [x] 原始资源大小读取：扫描时从 `PHAssetResource` 获取原始资源的已知 `dataSize`；大小未知时不显示原件大小，且不得根据像素、时长或估算码率虚构 iCloud 原件大小。汇总时只计算目标原始资源，不能把编辑副本、调整数据、Live Photo 等多个资源重复相加。
- [x] iCloud 本地状态探测：使用 PhotoKit 的本地优先请求（`isNetworkAccessAllowed = false`）识别“本地可用”“需要 iCloud 下载”和“状态未知”，扫描阶段不得因为读取大小或编码信息自动下载原件。
- [x] 原件读取与媒体分析：任务开始后使用 `PHAssetResourceManager` 获取目标原始资源并写入受控临时文件；下载完成后读取实际文件大小，再用 AVFoundation 检查视频编码、码率、音视频轨道和容器元数据。不得仅凭 UTI/文件扩展名判断 `avc1`、`hvc1` 等编码。
- [x] iCloud 体积与空间检查：云端项目进入任务后，以实际下载字节数重新核对本机可用空间、临时工作空间和实际输出；下载前只展示已知大小和“待下载确认”，下载后更新任务汇总及空间统计。
- [x] 视频编码默认路径：将 `AVAssetExportSession` 的 HEVC 预设作为默认“自动”路径；将 VideoToolbox 手动码率、码率策略、硬上限、关键帧和帧重排等参数移到“编辑参数”中的次要手动选项。主流程和摘要只显示“自动”，只有用户显式选择手动时才使用手动编码路径。
- [x] 媒体类型参数显示：视频类媒体类型只显示视频相关参数（编码器、码率、帧率和音频等）；照片类媒体类型只显示照片相关参数（格式、质量和像素尺寸等），隐藏不适用的选项，并同步更新参数摘要与实际结果流程。
- [x] Toolbar 标题信息布局：使用原生窗口标题显示当前媒体类型，使用原生窗口副标题显示项目数量；不将标题放入按钮或 Toolbar 气泡控件中；切换媒体类型或筛选结果后实时更新。
- [x] 手动视频码率档位与推荐算法：切换到手动模式后，用可编辑数值表按“分辨率 × 帧率”设置码率，分辨率显示为 `1080p`、`2160p`、`4320p`，列为 `30 fps` 和 `60 fps`，单位为 Mbps；默认 30 fps 码率分别为 `6`、`20`、`80`，60 fps 对应翻倍为 `12`、`40`、`160`。标准档位允许宽度或高度出现 1 像素偏差仍归入对应档位；非标准分辨率使用实际像素数 `P = width × height` 计算推荐值：以 `(2,073,600, 6)`、`(8,294,400, 20)`、`(33,177,600, 80)` 为锚点，在相邻锚点间按对数像素数/码率插值，低于 1080p 和高于 4320p 按像素比例延展，最后按 `fps / 30` 缩放。每个表格数值均可手动调整。
- [x] 移除预计节省数据：扫描、索引、筛选排序、选择栏、空间预检、队列摘要和其他主流程不再生成或展示“预计新大小”“预计节省”或“预计节省比例”；只在任务实际下载、压缩并验证完成后记录和展示真实原件大小、真实输出大小与真实节省空间，相关的预排除逻辑同步取消或延后到实际结果。
- [x] 用户界面文案审计：将面向用户的提示、说明、空状态、进度、错误和任务结果统一改写为简洁易懂的用户语言；移除 PhotoKit、资源读取、编码 sample entry、内部校验、码率实现等开发者实现细节，只保留用户需要理解和做决定的信息。
- [x] 自动视频设置界面：视频选择“自动”时只显示自动选项；手动码率、音频和高级控制仅在显式选择“手动”后展开。
- [x] 任务安全默认值：新建设置和旧版未修改过的默认设置使用 8% 最低实际节省比例；用户主动保存的其他比例保持不变。

#### 仍需产品负责人确认的决策

1. 首版是否只处理普通 JPEG 和 H.264 SDR，其他类型只扫描和报告？建议：是。
2. 首版是否排除所有已编辑资产？建议：是。
3. 是否允许选择隐藏相簿中的媒体？建议：默认排除，用户主动开启。
4. 是否把屏幕录制作为推荐压缩类型？建议：是，但单独分类。
5. 发布目标是 Mac App Store、Developer ID 独立发行，还是两者都支持？建议：先 Developer ID Beta，再评估商店限制。

## 8. Release

### Phase 0: 技术与产品验证（约 1 个迭代）

- 固定签名和权限模型。
- 建立 PhotoKit 扫描索引。
- 验证 JPEG -> HEIC。
- 完善 H.264 -> HEVC 验证器。
- 验证 iCloud 原件下载进度和取消。
- 建立两阶段替换状态机、独占检查会话与中断恢复。
- 校准照片质量、视频手动平均码率和速率上限的初始推荐参数。

退出条件：使用测试图库完成不少于 100 个普通资产，零不可恢复删除；重启后可以恢复 `review_pending` 并完成提交或撤回。

### Phase 1: 内部 Alpha（约 2 至 3 个迭代）

- SwiftUI 首页、照片浏览器、排除菜单、时间/大小筛选、排序、网格/列表、项目详情、处理队列和历史。
- 支持 JPEG 与普通 H.264 SDR。
- 任务结束后自动创建并验证压缩资产，随后强制用户选择撤回压缩副本或删除原件。
- 支持待检查会话锁定、双重退出确认和跨重启恢复。
- 支持推荐参数摘要和详细设置 Sheet。
- 支持拍摄日期、位置、收藏、隐藏和普通相簿验证。
- 支持本地媒体和需要 iCloud 下载的媒体。
- 简体中文和英文。

退出条件：核心用户流程完整，所有失败均可解释并恢复。

### Phase 2: External Beta（约 1 至 2 个迭代）

- 性能优化和大型图库增量扫描。
- 质量与节省估算校准。
- 权限、睡眠、网络中断、磁盘不足和 Photos 同步测试。
- Developer ID 签名、公证和自动更新方案。
- 隐私政策、诊断导出和用户帮助。

退出条件：达到第 4 节的 Beta 门槛。

### Phase 3: v1.0

P0 必须包含：

- 普通 JPEG 照片压缩。
- 普通 H.264 SDR 视频压缩。
- 扫描、排除、时间/大小筛选、排序、网格/列表、预估、预览、队列、验证、恢复和历史。
- 推荐参数与可编辑的详细压缩设置。
- 自动导入后的强制检查会话，以及“撤回压缩副本/确认删除原件”两阶段提交。
- 会话未解决时锁定下一任务，并支持退出和重启恢复。
- 稳定签名和完整权限体验。

后续版本候选：

- HDR/Dolby Vision 安全转码。
- Live Photo 成对资源处理。
- 已编辑资产的“保留编辑”或“扁平化副本”工作流。
- 高级质量与码率控制。
- TIFF 等旧静态图格式。
- 更丰富的相簿与导出报告。

### 发布门槛

- 没有任何绕过 PhotoKit 或直接修改 Photos 数据库的代码。
- 任何删除前都经过文件验证和 PhotoKit 元数据验证。
- 所有删除仅进入“最近删除”。
- 已完成主动退出、强制退出、崩溃、系统重启、权限拒绝、iCloud 断网和磁盘不足测试。
- 未解决的检查会话在任何情况下都能恢复，并且确实阻止新任务启动。
- 取消默认排除项时始终出现对应风险提醒，明确不支持的项目不能被强制处理。
- 用户在确认替换前能看到不可保留的数据清单。
- 应用已经稳定签名并公证，升级不会反复触发授权。
